# 首页金刚位 Icon 配置 — 需求说明

| 版本 | 日期 | 修订人 |
|------|------|--------|
| V1.0 | 2026-07-21 | Rhys |

## 一句话
运营在中台配置**首页金刚位**（推荐 / 理财 / AI美股通 / Launchpad 那一排入口）的**图标、文案、跳转链接、排序、显示开关**，App 拉取后渲染，改动无需发版。

## 为什么
金刚位入口现在写死在代码里，换图标、改文案、调顺序、上下线一个入口都要发版（3~7 天）。运营需要能自助、快速地调整这排入口。

## 范围
- ✅ 只做首页金刚区这一排入口图标（默认 4~5 个）
- ❌ 不做全 App 换肤、不做启动图标、不做多端主题包

## 配置项（每个金刚位一条）
| 字段 | 说明 | 必填 |
|------|------|:--:|
| 图标·亮色 | 亮色模式下的图标（建议 120×120 @3x，png/webp，≤100KB，透明底） | 是 |
| 图标·暗色 | 暗色模式下的图标；未上传则回落亮色图并给出"暗色·缺"提示 | 建议 |
| 名称（多语言） | 每种语言分别配置文案（建议 ≤5 字/词），App 按用户语言取对应文案，见下节 | 是（默认语言必填） |
| 跳转链接 | 点击后跳转的 URL / 路由（原生页 or H5） | 是 |
| 排序 | 显式序号 + 上移/下移，决定在金刚区从左到右的先后 | 是 |
| 显示开关 | 上线 / 下线该入口 | 是 |
| 生效时间 | 可选：定时上下线（不填=立即长期生效） | 否 |

## 多语言适配（名称）
App 支持多语言切换，金刚位**名称需按语言分别配置**（图标、跳转链接与语言无关，各语言共用一份）。
- 中台提供**语言切换**，运营逐语言填写每个金刚位的名称；切换语言时右侧预览同步显示该语言效果。
- **默认语言（简体中文）必填**；其余语言选填，缺失时 App **回落默认语言**文案渲染，不留空。
- 中台对缺失做提示：缺失语言的输入框标红「将回落默认语言"XXX"」，语言切换按钮上带 ● 标记提醒该语言仍有金刚位未配置，便于运营查漏补齐。
- 语言清单与 App 现有语言保持一致（当前示例：简体中文 / English / 繁體中文，实际以 App 支持语言为准）。
- 建议各语言文案控制在 5 字/词内，避免金刚位文字过长换行或截断。

> ⚠️ 三个维度是正交的：**语言**只影响名称，**亮/暗**只影响图标，**跳转链接**两者都不随之变化。一个金刚位 = 1 份链接 + 亮/暗两张图 + N 种语言名称。

## 黑白（暗色）模式适配
App 已支持亮色/暗色两套主题，金刚位图标需**成对上传**：
- 每个金刚位分别上传「亮色图标」与「暗色图标」，App 按系统/App 当前主题选用对应版本。
- 暗色图标为**建议项**：未上传时回落亮色图渲染，中台在该条标注「暗色·缺」提醒运营补齐（避免深色背景下浅色图标看不清/白边突兀）。
- 两版图标规格一致（120×120 @3x、透明底）；建议暗色版做描边或提亮处理。
- 中台预览支持亮/暗一键切换，发布前可分别核对两种模式效果。

## 数量与展示方式
- **App 端金刚区横向滑动展示，不设死数量上限**（对齐 Binance/OKX 做法）。
- 首屏露出约 **4.5 个**，露出半个引导用户左右滑动；下方可加滑动指示点。
- 数量为**软约束**：建议 ≤10 个以保证可发现性，中台超出时提示精简，但不禁止。
- 至少保留 1 个显示中的入口。

## 流程
```mermaid
flowchart LR
    A[运营配置金刚位:图标/文案/链接/排序/开关] --> B[实时预览] --> C[发布]
    C --> D[App 启动/回前台拉取列表] --> E[按排序渲染金刚区]
    E -.拉取失败/图标加载失败.-> F[回落上次缓存/内置默认]
```

## App 端规则
- 启动或回到前台时拉取金刚位列表，按 `排序` 渲染。
- 只渲染「显示开关=开」且在生效时间内的入口。
- 拉取失败用本地缓存，缓存也没有则用包内置默认配置 —— **不留白、不报错**。
- 单个图标加载失败只影响该格（显示占位/默认图），不影响整排。

## 校验与异常
- 图标：格式/尺寸/大小超限，上传即拦截。
- 跳转链接：格式校验；发布前可点"测试跳转"。
- 至少保留 1 个显示中的入口，不允许全部下线。
- 数量超过建议值（10 个）时提示精简，不做硬性拦截。

## 埋点
- `kingkong_exposure`：金刚位曝光（slot_name、position、jump_url）
- `kingkong_click`：点击（slot_name、position）
- `kingkong_config_publish`：中台发布（operator、item_count）

## 待确认
1. ~~固定格子数~~ → 已定：横向滑动、数量软约束（≤10 建议值）。
2. 是否需要"生效时间"定时上下线，还是只要手动开关？
3. 跳转是否要区分 iOS/Android/H5 不同链接？
4. 是否需要审核，还是运营直接发布？
