# 规则台账 · 账号切换（多账号管理）

> 唯一事实源。已确认规则以本表为准，不依赖对话记忆。

## 一、已确认规则表

| 编号 | 规则描述（业务语言） | 确认日期 | 备注 |
|---|---|---|---|
| R1 | 一个设备最多保存 **25 个**可切换账号；达上限时「Add a new account」按钮置灰不可点，需先删除才能再加 | 2026-08-24 | 列表底部展示 `n/25` 计数提示 |
| R2 | 点击非当前、未过期账号即完成切换，切换成功后返回 My Account，页面数据（UID/昵称/费率）同步刷新 | 2026-08-24 | 无需二次确认 |
| R3 | 当前账号带 `Current` 绿标；点击当前账号无切换动作 | 2026-08-24 | |
| R4 | 过期账号带 `Expired` 灰标；点击弹「重新登录」确认，确认后进登录页 | 2026-08-24 | 不静默拦截 |
| R5 | 「Add a new account」进登录页，登录成功后自动加入切换列表，**不退出当前账号** | 2026-08-24 | |
| R6 | 右上角编辑（笔）图标进入删除态：可删账号显示垃圾桶，底部按钮变 `Done` | 2026-08-24 | |
| R7 | 删除账号需**二次确认**弹窗（Confirm deletion?） | 2026-08-24 | |
| R8 | **当前登录账号不可删除**：删除态下当前账号不显示垃圾桶、仍显示 `Current` 标识；退出当前账号只能走 My Account 的 `Log Out` | 2026-08-24 | 职责分离：切换列表管多账号，Log Out 管当前会话 |
| R9 | **过期账号可删除**（用户可自行清理过期账号） | 2026-08-24 | 2026-08-24 确认采用「方法1」 |
| R10 | `Log Out` 需二次确认；退出后当前账号从设备移除，需重新登录 | 2026-08-24 | |
| R11 | 切换列表按 Binance 式分组：**Current Account**（当前账号置顶、带勾选标识）+ **Other Accounts**（其余账号） | 2026-08-24 | 对齐 Binance App Switch Account |
| R12 | 每个账号行展示：头像（昵称首字母，当前账号金色描边）+ **昵称 + 脱敏登录方式**（邮箱/手机号掩码）；**列表不展示 UID**（UID 仅在 My Account 详情页展示，可复制） | 2026-08-24 | 2026-08-24 调整：列表去掉 UID |
| R13 | 仅做多登录账号互切，**不引入子账号(Sub-account)体系**；个人账号合规上遵循一人一实名账号 | 2026-08-24 | 2026-08-24 确认不引入子账号 |
| R19 | **Log Out 与切换/添加对列表的差异**：Log Out 会把该账号从设备移除，退出后**不保留在切换列表**（如 A 登录后 Log Out 再登录 B，列表仅有 B）；若希望账号保留在列表随时切回，应使用「切换 / 添加」而非 Log Out。二者路径分工：Log Out=彻底退出并清除，切换/添加=保留多账号共存 | 2026-08-25 | R10 的补充说明；共享设备退出需清除该账号痕迹 |
| R18 | **所有账号均可加入切换列表**：包括未完成 KYC 的账号，添加环节不做 KYC 拦截 | 2026-08-25 | 2026-08-25 确认 |
| R17 | **过期账号保留在列表、系统不自动移除**：账号被置为 Expired 后仍保留在切换列表并展示 Expired 标，由用户重新验证激活或手动删除；定时任务只负责置 Expired，不做二级自动清理/移除 | 2026-08-25 | 本期范围仅 App 端；不影响其他业务线既有逻辑 |
| R16 | **同一账号可重复添加，列表去重仅保留一条**：允许用户重复登录同一账号，但切换列表按账号去重，仅展示一条（重复登录视为刷新该账号会话，定位到已有行，不新增重复项） | 2026-08-25 | 2026-08-25 确认；另确认**当前无禁用账户机制**，账号切换不涉及禁用态呈现 |
| R15 | **7 天未登录需重新验证**：切换列表中的账号若连续 **7 天未登录（无活跃）**，其本地登录状态判定为过期（Expired），不能直接切换；点击该账号弹「需要重新验证」，用户完成验证后方可使用。**验证方式 = 全量登录（重新输入密码 + 2FA），与全新登录一致**。计时口径 = 距该账号最近一次登录/活跃时间，**具体计时口径由前端定义**（前端本地掌握各账号会话时间） | 2026-08-25 | 2026-08-25 确认全量登录；计时口径归前端 |
| R14 | **登录方式脱敏口径**：① 邮箱 = 前 3 位 + `***` + 完整 @尾缀（如 `rhy***@gmail.com`）；② 手机号 = **区号 + 前 3 位 + 固定 3 星 + 后 3 位**，中间一律脱敏、星数固定为 3（如 `+86 134***234` / `+1 516***797` / `+65 812***567` 8 位短号同规则）；**极短号码兜底**：国家码后号码不足 7 位时退化为「后 2 位 + 前面全脱敏」（如 `+65 ***67`），确保任何长度都不露出完整号码 | 2026-08-24 | 区号按已知码表匹配；手机口径「前3+后3」；2026-08-25 补极短号码兜底 |

## 二、待确认清单

| 编号 | 问题描述 | 阻塞的下游工作 |
|---|---|---|
| Q1 | UID 后的复制、昵称编辑、费率详情是否属于本次范围 | 原型中为示意，PRD 是否纳入 |
| ~~Q2~~ | ~~账号列表脱敏规则~~ 已确认，见 R14 | ✅ 2026-08-24 结项 |
| Q3 | 删除全部账号后的兜底（是否回登录页）在 R8 下已不会触发（当前账号不可删），是否仍需定义极端场景 | 边界 case 表 |

## 三、版本历史表

| 版本 | 日期 | 变更摘要 | 变更原因 |
|---|---|---|---|
| v1.0 | 2026-08-24 | 建立台账，登记 R1–R10 | 账号切换原型规则收敛 |
| v1.1 | 2026-08-24 | 新增 R11–R13（Binance 式分组/头像/不引入子账号） | 按主流交易所(Binance)对齐切换形态 |
| v1.2 | 2026-08-24 | 调整 R12：切换列表去掉 UID，仅展示昵称 + 脱敏登录方式 | 精简列表信息，UID 归入详情页 |
| v1.3 | 2026-08-24 | 新增 R14 脱敏口径；Q2 结项 | 确认邮箱/手机脱敏统一规则 |
| v1.4 | 2026-08-24 | R14 手机口径改为「区号 + 前3 + 3星 + 后3」 | 展示前三后三、中间固定脱敏 |
| v1.5 | 2026-08-25 | 新增 R15：账号 7 天未登录需重新验证 | 明确 Expired 触发条件与安全口径 |
| v1.6 | 2026-08-25 | 新增 R16（重复添加去重）；R14 补极短号码兜底；确认无禁用账户机制 | PM 定稿边界 Case B6/B7/B8 |
| v1.7 | 2026-08-25 | 新增 R17（过期账号不自动移除）；明确本期仅 App 端、不影响其他业务线 | PM 定稿 Checklist #4/#7 与失效会话清理策略 |
| v1.8 | 2026-08-25 | R15 明确重新验证方式为全量登录（密码 + 2FA） | PM 确认 Q-01 校验强度 |
| v1.9 | 2026-08-25 | 新增 R18（所有账号可入列，不做 KYC 拦截）；R15 计时口径归前端定义 | PM 确认 KYC 与计时口径归属 |
| v1.10 | 2026-08-25 | 新增 R19（Log Out 退出不保留在切换列表，与切换/添加区分） | PM 询问 A 登出后再登录 B 的列表场景 |
