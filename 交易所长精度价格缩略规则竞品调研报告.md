# 交易所长精度代币价格缩略规则 — 竞品调研报告

- **调研日期**：2026-07-07
- **调研目标**：为价格精度较长的代币（如 BabyDoge ≈ 0.0000000003、PEPE ≈ 0.0000026）制定一套价格缩略展示规则
- **样本币种**：BABYDOGE（9 个前导零）、PEPE / SHIB（5 个前导零）
- **调研对象**：Binance、OKX、Bybit、Gate、Bitget、MEXC + 数据平台（CoinMarketCap、CoinGecko、DexScreener）+ TradingView / TabTrader
- **调研方式**：实际抓取各平台页面对样本币种的真实渲染字符串 + 官方公告/文档检索

---

## 一、问题定义

Meme 币价格常见 5~12 个前导零（BabyDoge = $0.000000000303）。全量展示存在三类问题：

1. **可读性**：人眼无法快速数清零的个数，0.000000000303 与 0.00000000303 相差 10 倍但几乎无法目视区分；
2. **空间**：行情列表、资产页宽度有限，13+ 位小数会溢出/换行，移动端尤甚；
3. **可比性**：涨跌幅、区间对比时长串零干扰主要信息。

业界已收敛出 **4 种方案**，各竞品在不同场景组合使用：

| 方案 | 示例（0.0000000003039） | 优点 | 缺点 |
|------|------|------|------|
| A. 全零展示 | 0.0000000003039 | 无歧义、可直接复制交易 | 不可读、占宽 |
| B. 下标折叠零 | 0.0₉3039 | 紧凑、零个数一目了然 | 需教育成本、图表轴/输入框不适用 |
| C. 乘数交易对 | 1MBABYDOGE = 0.0003039 | 从根源消除问题，交易全链路正常 | 需另发交易对、用户换算心智 |
| D. 截断/科学计数 | <$0.000001 / 3.04e-10 | 简单 | 丢失信息（截断）/ 普通用户不懂（科学计数） |

---

## 二、竞品逐家拆解（含实测证据）

### 1. Binance（币安）
- **官网币价页（SEO 页）**：直接截断为 `<$0.000001`，不展示具体值（实测 BabyDoge 页面）；
- **现货**：不直接上架超长精度币对，改用**乘数锚定币**：`1MBABYDOGE`（=100 万枚 BabyDoge）、`1000SATS`、`1000CAT` 等，价格回到正常精度区间；
- **合约**：同样用乘数合约 `1000PEPE`、`1000BONK`、`1000SHIB`、`1MBABYDOGE` 永续；
- **Alpha/Web3 行情**：链上代币无法发乘数对，App 内使用下标折叠展示 **[待实机验证]**；
- **结论**：Binance 的主策略是**方案 C（乘数交易对）**，让交易全链路（订单簿、下单、K 线）都不面对长零，展示层截断兜底。

### 2. OKX（欧易）
- **币价/行情信息页**：**下标折叠**，实测渲染 `$0.0₉303`（保留 3~4 位有效数字），ATH 同样 `$0.0₈669`；
- **交易区**：实测同页面交易对报价仍为全零 `0.00000000030300`（订单簿/成交遵循 tick size 全量展示）;
- **结构化数据（schema）**：科学计数 `3.03e-10`（仅机器可读层）；
- **结论**：**展示层折叠、交易层全量**的双层策略，是 CEX 中下标方案落地最完整的。

### 3. Bybit
- **现货交易页 & 币价页**：**全零展示**，实测 `$0.000000000373215`（15 位小数，可读性差）；
- **合约**：用乘数合约处理（1000 系列）；
- **结论**：展示层未做缩略优化，属于对照组，也说明全零方案在 9 零级别体验明显劣化。

### 4. Gate
- **现货交易页**：**全零展示**，实测页面标题即 `0.0000000004176 | BABYDOGE/USDT`；
- **合约**：`MBABYDOGE`（=1,000,000×BABYDOGE）永续，官方公告明确说明乘数目的是"让合约价格更易展示和交易"；
- **结论**：现货全零 + 合约乘数。

### 5. Bitget
- **币价页**：**折叠零**，实测渲染 `$0.{9}3039`（UI 上为下标样式，保留 4 位有效数字），ATH `$0.{8}6604`；同页支撑/阻力位等技术指标仍全零 `$0.000000001950`；
- **结论**：与 OKX 同路线：营销/行情层折叠，技术数据层全量。

### 6. MEXC
- **币价页**：**全零展示**，实测 `$0.0000000003042`，全站一致不折叠；
- **结论**：以"上币多、长尾币多"著称却未做缩略，长尾币列表可读性差，属对照组。

### 7. 数据平台（事实标准的风向标）
- **CoinMarketCap**：**下标折叠**。实测 PEPE `$0.0₅2681`、SHIB `$0.0₅4360`、BabyDoge `$0.0₉3042`；**关键阈值证据**：ATH `$0.00002825`（4 个零）仍全量展示 → **触发阈值为前导零 ≥5 个，保留 4 位有效数字**；
- **CoinGecko**：与 CMC 完全一致的规则。实测 PEPE `$0.0₅2682`，ATH `$0.00002803`（4 零不折叠）；且支持**两位数下标**：ATL `$0.0₁₀9477`（10 个零）；
- **DexScreener**：**下标折叠**（`$0.0₅1595`），链上工具（GMGN、Birdeye、Ave 等 meme 终端）均已采用同类记法，是 meme 交易人群的既有心智；
- **TradingView**：K 线价格轴**不支持**下标渲染，仅有社区指标以"科学计数/下标"重排显示 → 印证 **K 线轴不适合做折叠**，这也是交易所普遍"图表用原值或乘数对"的原因；
- **TabTrader**：将"折叠小数零（shrink fractional zeros）"做成**用户可开关设置**，并允许自定义触发折叠的最少零个数。

---

## 三、横向对比矩阵

| 平台 | 行情/币详情 | 订单簿/下单/成交 | K线 | 合约处理 | 触发阈值 | 有效位数 |
|------|------|------|------|------|------|------|
| Binance | 截断 `<$0.000001`（站外页）；Alpha 折叠[待验证] | 乘数对，正常精度 | 乘数对，正常精度 | 1000x/1M 乘数 | — | — |
| OKX | ✅ 下标 `0.0₉303` | 全零（tick 精度） | 原值 | 乘数（1000 系列） | 观测 ≥5 零 | 3~4 位 |
| Bybit | ❌ 全零 | 全零 | 原值 | 1000x 乘数 | 不折叠 | — |
| Gate | ❌ 全零 | 全零 | 原值 | M（100 万倍）乘数 | 不折叠 | — |
| Bitget | ✅ 下标 `0.0₉3039` | 全零 | 原值 | 乘数 | 观测 ≥5 零 | 4 位 |
| MEXC | ❌ 全零 | 全零 | 原值 | 乘数 | 不折叠 | — |
| CMC | ✅ 下标 `0.0₅2681` | — | — | — | **≥5 零（4 零不折叠）** | **4 位** |
| CoinGecko | ✅ 下标，支持 `0.0₁₀9477` | — | — | — | **≥5 零** | 4 位 |
| DexScreener/GMGN 等 | ✅ 下标 `0.0₅1595` | — | 部分原值 | — | 约 ≥4~5 零 | 4 位 |

> 图例：✅ 已做缩略优化 / ❌ 全零展示。"观测"为基于实测样本的推断。

**行业趋势判断**：下标折叠已经由 DEX 工具 → 数据平台 → 头部 CEX（OKX/Bitget）逐级渗透，成为事实标准；未做的（Bybit/Gate/MEXC）属于滞后而非另有方案。所有平台的共同点是：**折叠只发生在"展示层"，交易层（订单簿、下单、K 线轴）从不折叠**，长零问题在交易层靠乘数交易对解决。

---

## 四、结论与建议

### ✅ 可借鉴清单

| # | 借鉴点 | 来源 | 落地建议 |
|---|--------|------|---------|
| 1 | 下标折叠记法 `0.0{N}dddd` | OKX / Bitget / CMC / CoinGecko / DexScreener | 行情列表、币种详情、资产估值、搜索结果采用 |
| 2 | 触发阈值：前导零 ≥5 才折叠；≤4 零全量展示 | CMC / CoinGecko（实测 ATH 4 零不折叠） | 与行业对齐，避免 0.0001 级别也被折叠造成过度设计 |
| 3 | 折叠后保留 4 位有效数字 | CMC / CoinGecko / Bitget | 统一 4 位；OKX 的 3 位偏少，涨跌时尾数变化不明显 |
| 4 | 展示层折叠、交易层全量的双层架构 | OKX / Bitget | 订单簿、下单输入、成交明细、K 线轴一律用原值（按 tick size） |
| 5 | 下标支持两位数（≥10 个零） | CoinGecko `0.0₁₀9477` | 渲染组件按位数自适应，不写死一位 |
| 6 | 合约/衍生品用乘数标的根治 | Binance 1000PEPE、Gate MBABYDOGE | 新上长精度币的合约直接按 1000x/1M 乘数设计 |

### 🚫 不可抄清单

| # | 做法 | 来源 | 不抄原因 |
|---|------|------|---------|
| 1 | 截断为 `<$0.000001` | Binance SEO 页 | 丢失价格信息，交易场景不可接受（仅适合站外营销页兜底） |
| 2 | 科学计数 `3.04e-10` 面向用户展示 | 各家仅用于 schema/机读层 | 普通用户不理解，没有任何竞品在 UI 主路径使用 |
| 3 | 全零展示到 13+ 位 | Bybit / Gate / MEXC | 实测可读性最差，属行业滞后做法 |
| 4 | 使用 Unicode 下标字符（₀₁₂…）存储/传输 | — | 字体覆盖不全、复制粘贴后语义混乱；应由前端样式渲染（`<sub>`/缩小字号），数据层始终传原始数值字符串 |
| 5 | K 线价格轴做折叠 | TradingView 原生不支持 | 图表库轴刻度不支持下标，强做成本高；轴用原值或跟随乘数对 |

### 💡 建议规则方案（可直接进 PRD）

**触发条件**：`0 < price < 0.0001` 且小数点后连续前导零个数 N ≥ 5。

**展示格式**：`0.0{N}dddd`，其中 `{N}` 为下标渲染的零个数（支持两位数），`dddd` 为 4 位有效数字（截断不进位，与 tick 对齐）。
示例：`0.000000000303954` → `0.0₉3039`；`0.00002825` → 不折叠，原样展示。

**应用范围**：
- ✅ 折叠：行情列表、币种详情页头部价格、资产折算价、搜索联想、涨跌推送文案、分享海报；
- ❌ 不折叠：订单簿、下单价格输入框、成交明细、委托/历史订单、K 线价格轴与 OHLC 数值、API 返回、导出对账单。

**交互规则**：
- 悬停 / 长按折叠价格 → 气泡展示完整原值；
- 点击复制 → 剪贴板写入完整原值（`0.000000000303954`），不是带下标的字符串；
- 排序、涨跌幅、报警判断一律基于原始数值，折叠仅发生在渲染最后一步；
- 价格波动导致 N 变化（如 0.0₅9998 → 0.0₄1000）时直接切换，无需动画特殊处理，但组件需保证宽度稳定不跳版。

**技术约定**：前端做一个统一的 `FormatSubZeroPrice` 组件/管道，输入原始字符串，输出富文本；禁止各业务线自行实现（否则阈值/位数会出现不一致——这正是本次调研里 Bybit/Gate 站内外不一致的教训）。

**可选增强（P2）**：参考 TabTrader，在设置中提供"折叠零显示"开关与阈值自定义，服务专业用户。

### 📋 行动清单

| 优先级 | 行动项 | 类型 | 说明 |
|--------|-------|------|------|
| P0 | 制定《长精度价格展示规范》并评审（阈值 ≥5 零、4 位有效数字、应用范围表） | 规范 | 本报告第四节可直接作为初稿 |
| P0 | 前端统一 FormatSubZeroPrice 组件（含复制原值、hover 全量、两位下标） | 功能 | Web/App 双端 |
| P1 | 行情列表、币详情、资产页接入 | 功能 | 首批落地场景 |
| P1 | 新上长精度币合约按乘数标的（1000x/1M）设计立项 | 机制 | 对齐 Binance/Gate |
| P2 | 设置项：折叠开关 + 自定义阈值 | 增强 | 参考 TabTrader |

---

## 五、信息来源与待验证项

**主要来源**（实测渲染 + 官方文档）：
- OKX BabyDoge 币价页（实测 `$0.0₉303`）：https://www.okx.com/price/baby-doge-coin-babydoge
- Bitget BabyDoge 币价页（实测 `$0.{9}3039`）：https://www.bitget.com/price/baby-doge-coin
- MEXC BabyDoge 币价页（实测全零）：https://www.mexc.com/price/BABYDOGE
- Bybit 现货 BABYDOGE/USDT（实测全零 `$0.000000000373215`）：https://www.bybit.com/trade/spot/BABYDOGE/USDT
- Gate 现货 BABYDOGE/USDT（实测全零 `0.0000000004176`）：https://www.gate.com/trade/BABYDOGE_USDT
- Binance BabyDoge 币价页（实测 `<$0.000001`）：https://www.binance.com/en/price/baby-doge-coin
- Binance 1000PEPE 永续上线公告：https://www.binance.com/en/support/announcement/detail/41993e3389654713946bcb6b9b032eaf
- Gate MBABYDOGE（1,000,000×）合约公告：https://www.gate.io/announcements/article/23492/gate.io-has-added-mbabydoge-1000000-x-babydoge-perpetual-contract
- CoinMarketCap PEPE / SHIB / BabyDoge 页（实测下标 + 阈值证据）：https://coinmarketcap.com/currencies/pepe/
- CoinGecko PEPE / BabyDoge 页（实测下标、两位下标 `0.0₁₀9477`）：https://www.coingecko.com/en/coins/pepe
- DexScreener（实测 `$0.0₅1595`）：https://dexscreener.com/
- TradingView 社区指标（图表轴不支持下标的旁证）：https://www.tradingview.com/script/bJscU7oQ-Readable-Format-Small-Unit-Price-Scientific-Subscript-Notation/
- TabTrader 设置文档（折叠开关 + 自定义阈值）：https://tabtrader.com/helpcenter/android/settings-and-profile/settings-page

**待验证项**：
1. Binance App 内 Alpha 代币行情是否使用下标折叠（本次为推断，需实机确认）；
2. OKX / Bitget **App 端**行情列表的折叠样式与 Web 是否一致；
3. 各家 App 对折叠价格的复制行为（复制原值还是带下标字符串）——建议实机抓包/体验确认后回填本报告。
